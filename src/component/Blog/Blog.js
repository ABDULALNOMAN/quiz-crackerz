import React from 'react';

const Blog = () => {
    return (
        <dl>
            <dt>what is the purpess of react router</dt> 
            <dd>React Router is a standard library for routing in React. It enables the navigation among views of various components in a React Application, allows changing the browser URL, and keeps the UI in sync with the URL</dd>
            <dt>how </dt> 
            <dd></dd>
            <dt></dt> 
            <dd></dd>
        </dl>
    );
};

export default Blog;